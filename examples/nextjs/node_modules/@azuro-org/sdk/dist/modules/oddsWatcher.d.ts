import { type OddsChangedData } from '../contexts/socket';
type Cb = (oddsData?: OddsChangedData) => void;
export declare const oddsWatcher: {
    subscribe: (conditionId: string, outcomeId: string, cb: Cb) => () => void;
    dispatch: (conditionId: string, oddsData?: OddsChangedData) => void;
};
export {};
