import { type TransactionReceipt, type Address } from 'viem';
import { type Selection, type PrematchBetFragment, type LiveBetFragment } from '@azuro-org/toolkit';
type UpdateBetProps = {
    coreAddress: Address;
    tokenId: string | bigint;
};
export type NewBetProps = {
    bet: {
        rawAmount: bigint;
        selections: Selection[];
        freebetId?: string | bigint;
        freebetContractAddress?: Address;
    };
    odds: Record<string, number>;
    affiliate: Address;
    receipt: TransactionReceipt;
};
export declare const useBetsCache: () => {
    updateBetCache: ({ coreAddress, tokenId }: UpdateBetProps, values: Partial<PrematchBetFragment> | Partial<LiveBetFragment>) => void;
    addBet: (props: NewBetProps) => Promise<void>;
};
export {};
