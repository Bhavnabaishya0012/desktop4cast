import { type Selection } from '@azuro-org/toolkit';
type CalcOddsProps = {
    selections: Selection[];
    betAmount?: string;
    batchBetAmounts?: Record<string, string>;
};
export declare const useOdds: ({ selections, betAmount, batchBetAmounts }: CalcOddsProps) => {
    odds: Record<string, number>;
    totalOdds: number;
    maxBet: number | undefined;
    loading: boolean;
};
export {};
