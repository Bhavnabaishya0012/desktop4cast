import React from 'react';
import { type ConditionStatus } from '@azuro-org/toolkit';
export type SocketContextValue = {
    isSocketReady: boolean;
    subscribeToUpdates: (conditionIds: string[]) => void;
    unsubscribeToUpdates: (conditionIds: string[]) => void;
};
export type SocketData = {
    id: string;
    state?: ConditionStatus;
    margin: number;
    reinforcement: number;
    winningOutcomesCount: number;
    outcomes?: Array<{
        id: number;
        odds: number;
        clearOdds: number;
        maxStake: number;
    }>;
}[];
export type OddsChangedData = {
    conditionId: string;
    margin: number;
    reinforcement: number;
    winningOutcomesCount: number;
    outcomes: Record<string, {
        odds: number;
        clearOdds: number;
        maxBet: number;
    }>;
};
export declare const useSocket: () => SocketContextValue;
export declare const SocketProvider: React.FC<any>;
