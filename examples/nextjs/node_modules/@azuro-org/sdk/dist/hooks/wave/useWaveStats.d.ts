import { type Address } from 'viem';
import { type WaveId } from '@azuro-org/toolkit';
type Props = {
    account: Address;
    waveId?: WaveId;
};
export declare const useWaveStats: ({ account, waveId }: Props) => import("@tanstack/react-query").UseQueryResult<{
    isActivated: boolean;
    waveId: number;
    initialLevel: number;
    level: number;
    betPoints: string;
    dexPoints: string;
    liqudityPoints: string;
    stakingPoints: string;
    leaderboardPoints: string;
    manualPoints: string;
    points: string;
    multipliedPoints: string;
    sharePercent: string;
    levelDescription: import("@azuro-org/toolkit").WaveLevelData;
} | null, Error>;
export {};
