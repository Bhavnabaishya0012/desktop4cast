import { type GameStatus, type LiveGraphGameStatus } from '@azuro-org/toolkit';
type GraphGameStatus = LiveGraphGameStatus;
type Props = {
    startsAt: number;
    graphStatus: GraphGameStatus;
    isGameExistInLive: boolean;
};
export declare const useGameStatus: ({ graphStatus, startsAt, isGameExistInLive }: Props) => {
    status: GameStatus;
    isGameStarted: boolean;
};
export {};
