import { type ApolloClient } from '@apollo/client';
import { type ConditionStatus } from '@azuro-org/toolkit';
type OutcomeData = {
    status: ConditionStatus;
    odds: number;
};
type Result = Record<string, OutcomeData>;
export declare const batchFetchOutcomes: (conditionEntityIds: string[], client: ApolloClient<object>) => Promise<Result>;
export {};
