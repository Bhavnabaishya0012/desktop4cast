import { type Address } from 'viem';
import { Bet_OrderBy, OrderDirection } from '@azuro-org/toolkit';
import { type Bet, BetType } from '../../global';
export type UsePrematchBetsProps = {
    filter: {
        bettor: Address;
        affiliate?: string;
        type?: BetType;
        limit?: number;
        offset?: number;
    };
    orderBy?: Bet_OrderBy;
    orderDir?: OrderDirection;
};
export declare const usePrematchBets: (props: UsePrematchBetsProps) => {
    loading: boolean;
    bets: Bet[];
    error: import("@apollo/client").ApolloError | undefined;
};
