import { type WaveId } from '@azuro-org/toolkit';
export type WavePeriod = {
    id: number;
    startsAt: number;
    endsAt: number;
    totalPoints: string;
    isBonusPreCalc: boolean;
};
type Props = {
    waveId?: WaveId;
};
export declare const useWavePeriods: ({ waveId }?: Props) => import("@tanstack/react-query").UseQueryResult<WavePeriod[] | null, Error>;
export {};
