import { GameStatus } from '@azuro-org/toolkit';
import { type FetchPolicy } from '@apollo/client';
type Props = {
    gameId: string;
    gameStatus: GameStatus;
    filter?: {
        outcomeIds?: string[];
        maxMargin?: number;
    };
    livePollInterval?: number;
    fetchPolicy?: FetchPolicy;
};
export declare const useActiveMarkets: (props: Props) => {
    loading: boolean;
    markets: import("@azuro-org/toolkit").GameMarkets;
    error: import("@apollo/client").ApolloError | undefined;
};
export {};
