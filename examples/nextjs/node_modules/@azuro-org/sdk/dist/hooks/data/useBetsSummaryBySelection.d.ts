import { type Address } from 'viem';
import { GameStatus } from '@azuro-org/toolkit';
export type BetsSummaryBySelection = Record<string, string>;
type Props = {
    account: Address;
    gameId: string;
    gameStatus: GameStatus;
    keyStruct?: 'outcomeId' | 'conditionId-outcomeId';
};
export declare const useBetsSummaryBySelection: ({ account, gameId, gameStatus, keyStruct }: Props) => {
    betsSummary: BetsSummaryBySelection;
    loading: boolean;
    error: import("@apollo/client").ApolloError | undefined;
};
export {};
